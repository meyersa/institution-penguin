name: Update container
on: [push]
jobs:
  build-deploy:
    runs-on: self-hosted
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Build docker
        run: docker compose build
      - name: Deploy docker
        run: docker compose up -d