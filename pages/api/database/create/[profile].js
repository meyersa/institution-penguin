/*
 * Create profile API endpoint
 * When a new user logs in this should be fetched 
 * Requires NextAuth session 
 * Creates profile with an incremented ID and either user input or Github pass in
 */ 